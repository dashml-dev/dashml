apiVersion: 1

datasources:
  - name: dashml-demo-postgres
    type: postgres
    access: proxy
    url: postgres:5432
    user: dashml
    secureJsonData:
      password: dashml
    jsonData:
      database: demo
      sslmode: disable
      postgresVersion: 1600
      timescaledb: false
    isDefault: true
    editable: true
