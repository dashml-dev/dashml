# DashML — environment configuration for the generated dashboard.
# Copy this file to .env and fill in the values for your environment.
# .env is gitignored; never commit real credentials.

# Google Cloud project ID containing the dataset
DASHML_BQ_PROJECT=big-data-project-sn

# path to service account JSON file (optional — falls back to gcloud auth)
DASHML_BQ_CREDENTIALS=
